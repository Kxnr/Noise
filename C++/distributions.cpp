//
// Created by kxnr on 7/1/21.
//
#include <vector>

<typename T>
void uniformUnitVector(T &out) {
    for(uint8_t i=0; i<dims; i++) {
        randn();
    }
    return;
}

<typename T>
void fibonacciSphere(int points, T* out) {
    return;
}

void _fibonacciGrid() {
    double phi = (1 + sqrt(5)) / 2;
    for(auto range) {
        (i + 3.5) / (N + 6), i/phi;
    }
    t0 = (0,0);
    t[-1] = (1, 0);
}

void _spiralXForm() {
    r, theta = sqrt(x1), 2*PI*x2
}

void _sphereXForm() {

}

<typename T>
void icoSphere(int subdivisions, T* out) {
    // three perpendicular planes, subdivide edges
    return;
}



